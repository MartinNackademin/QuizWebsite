import React from 'react';
import QuizContainer from '../../Quizcomponent/QuizContainer.jsx';

export const Quiz = () => {
    return (
        <section>
            <QuizContainer/>
        </section>
    );
};