import React from 'react';
import QuizComponent from '../../Quizcomponent/Quizcomponent';

export const Quiz = () => {
    return (
        <section>
            <QuizComponent />
        </section>
    );
};